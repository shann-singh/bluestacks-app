import React from "react";
import { Navbar, Nav } from "react-bootstrap";

const Header = () => {
  return (
    <Navbar bg='light' expand='lg'>
      <Navbar.Brand href='/'>Bluestacks Full Stack Web App</Navbar.Brand>
    </Navbar>
  );
};

export default Header;
